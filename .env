# Uncomment 2 following lines for desktop environment without cert, otherwise the site will be served with https and dev nginx conf
USE_PORT_80=80:80
NGINX_CONF=./desktop.nginx.conf

# Uncomment to use the built in python dev web server which will reflect changes in the code immediately, otherwise the server will be started with gunicorn
#BUILT_IN_SERVER_CMD=/bin/bash -c "source activate gaia && python manage.py runserver 0.0.0.0:8000"
